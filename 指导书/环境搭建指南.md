# Agent开发环境搭建指南

## 1. 开发环境总览

```
┌─────────────────────────────────────────────────────────┐
│                Agent开发环境架构                         │
├─────────────────────────────────────────────────────────┤
│  🖥️ 本地开发环境                                         │
│     - Python/Node.js 运行时                             │
│     - IDE配置 (VSCode/PyCharm)                          │
│     - 本地LLM (Ollama/LocalAI)                          │
│                                                         │
│  ☁️ 云服务依赖                                           │
│     - LLM API (OpenAI/Claude/国产大模型)                 │
│     - 向量数据库 (Pinecone/Milvus/Chroma)               │
│     - 监控平台 (LangSmith/Phoenix)                       │
│                                                         │
│  🐳 容器化环境                                           │
│     - Docker + Docker Compose                           │
│     - 开发/测试/生产环境隔离                              │
└─────────────────────────────────────────────────────────┘
```

---

## 2. Python环境搭建

### 2.1 基础环境（推荐）

```bash
# 1. 安装Python 3.10+ (推荐3.11)
# Windows: 从python.org下载安装
# Mac: brew install python@3.11
# Linux: sudo apt install python3.11

# 2. 创建虚拟环境
python -m venv agent-env

# Windows激活
agent-env\Scripts\activate

# Mac/Linux激活
source agent-env/bin/activate

# 3. 升级pip
pip install --upgrade pip
```

### 2.2 核心依赖安装

```bash
# === 基础框架 ===
pip install langchain langchain-core langchain-community
pip install llama-index
pip install semantic-kernel  # 微软的框架

# === LLM API客户端 ===
pip install openai anthropic
pip install dashscope  # 阿里云通义
pip install zhipuai    # 智谱AI
pip install qianfan    # 百度千帆

# === 向量数据库 ===
pip install chromadb faiss-cpu pinecone-client
pip install milvus pymilvus

# === 工具调用 ===
pip install requests httpx aiohttp
pip install beautifulsoup4 playwright
pip install python-dotenv pydantic

# === 开发调试 ===
pip install jupyter notebook
pip install pytest pytest-asyncio
pip install rich tqdm

# === Web服务（可选）===
pip install fastapi uvicorn
pip install streamlit gradio
```

### 2.3 requirements.txt模板

```txt
# requirements.txt - Agent开发核心依赖

# === LangChain生态 ===
langchain==0.2.0
langchain-core==0.2.0
langchain-community==0.2.0
langchain-openai==0.1.0
langchain-anthropic==0.1.0

# === LlamaIndex ===
llama-index==0.10.0
llama-index-llms-openai==0.1.0

# === LLM API ===
openai==1.12.0
anthropic==0.18.0
dashscope==1.14.0

# === 向量数据库 ===
chromadb==0.4.22
faiss-cpu==1.7.4
pinecone-client==3.0.0

# === 工具与工具箱 ===
pydantic==2.5.0
python-dotenv==1.0.0
httpx==0.26.0
aiohttp==3.9.0
beautifulsoup4==4.12.0
playwright==1.41.0

# === 开发工具 ===
pytest==8.0.0
pytest-asyncio==0.23.0
rich==13.7.0
tqdm==4.66.0

# === Web框架（可选）===
fastapi==0.109.0
uvicorn==0.27.0
streamlit==1.31.0
gradio==4.16.0
```

---

## 3. Node.js环境搭建

### 3.1 基础环境

```bash
# 1. 安装Node.js 18+ (推荐20 LTS)
# 从nodejs.org下载安装

# 2. 验证安装
node --version  # v20.11.0+
npm --version   # 10.0.0+

# 3. 使用pnpm（更快的包管理器）
npm install -g pnpm

# 4. 使用nvm管理多版本（可选）
# Windows: nvm-windows
# Mac/Linux: nvm
```

### 3.2 核心依赖安装

```bash
# === LangChain.js ===
pnpm add langchain @langchain/core @langchain/openai

# === LLM SDK ===
pnpm add openai @anthropic-ai/sdk

# === 向量数据库 ===
pnpm add @pinecone-database/pinecone chromadb

# === 工具库 ===
pnpm add zod dotenv axios cheerio
pnpm add @mozilla/readability jsdom

# === Web框架 ===
pnpm add express fastify next.js

# === 开发工具 ===
pnpm add -D typescript @types/node ts-node nodemon
pnpm add -D vitest jest
```

### 3.3 package.json模板

```json
{
  "name": "agent-project",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "langchain": "^0.1.0",
    "@langchain/core": "^0.1.0",
    "@langchain/openai": "^0.0.19",
    "openai": "^4.28.0",
    "@anthropic-ai/sdk": "^0.19.0",
    "zod": "^3.22.0",
    "dotenv": "^16.4.0",
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "vitest": "^1.2.0"
  }
}
```

---

## 4. IDE配置

### 4.1 VSCode推荐配置

```json
// .vscode/settings.json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll": true
  },
  "python.defaultInterpreterPath": "${workspaceFolder}/agent-env/bin/python",
  "python.linting.enabled": true,
  "python.linting.pylintEnabled": true,
  "python.formatting.provider": "black",
  "[python]": {
    "editor.formatOnSave": true,
    "editor.defaultFormatter": "ms-python.black-formatter"
  },
  "[typescript]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

### 4.2 VSCode推荐扩展

```json
// .vscode/extensions.json
{
  "recommendations": [
    // Python开发
    "ms-python.python",
    "ms-python.black-formatter",
    "ms-python.pylance",
    
    // TypeScript/JavaScript
    "esbenp.prettier-vscode",
    "dbaeumer.vscode-eslint",
    
    // AI/LLM相关
    "codeium.codeium",
    "github.copilot",
    
    // 工具
    "eamodio.gitlens",
    "ms-azuretools.vscode-docker",
    "redhat.vscode-yaml"
  ]
}
```

### 4.3 调试配置

```json
// .vscode/launch.json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Python: Current File",
      "type": "python",
      "request": "launch",
      "program": "${file}",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env"
    },
    {
      "name": "Python: FastAPI",
      "type": "python",
      "request": "launch",
      "module": "uvicorn",
      "args": [
        "main:app",
        "--reload"
      ],
      "jinja": true,
      "envFile": "${workspaceFolder}/.env"
    },
    {
      "name": "TypeScript: Debug",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "tsx",
      "runtimeArgs": ["${file}"],
      "console": "integratedTerminal"
    }
  ]
}
```

---

## 5. 环境变量配置

### 5.1 .env模板

```bash
# .env - API密钥配置（不要提交到Git！）

# === OpenAI ===
OPENAI_API_KEY=sk-xxx
OPENAI_API_BASE=https://api.openai.com/v1  # 或代理地址

# === Anthropic Claude ===
ANTHROPIC_API_KEY=sk-ant-xxx

# === 国产大模型 ===
# 阿里云通义
DASHSCOPE_API_KEY=sk-xxx

# 智谱AI
ZHIPUAI_API_KEY=xxx

# 百度千帆
QIANFAN_AK=xxx
QIANFAN_SK=xxx

# === 向量数据库 ===
PINECONE_API_KEY=xxx
PINECONE_ENVIRONMENT=us-east-1

# === 其他服务 ===
SERPER_API_KEY=xxx  # 搜索API
BROWSERLESS_API_KEY=xxx  # 浏览器自动化

# === 应用配置 ===
APP_ENV=development
LOG_LEVEL=DEBUG
```

### 5.2 配置加载

```python
# config.py - Python配置管理
from pydantic_settings import BaseSettings
from typing import Optional

class Settings(BaseSettings):
    # LLM配置
    openai_api_key: str
    openai_api_base: str = "https://api.openai.com/v1"
    anthropic_api_key: Optional[str] = None
    
    # 国产模型
    dashscope_api_key: Optional[str] = None
    zhipuai_api_key: Optional[str] = None
    
    # 向量数据库
    pinecone_api_key: Optional[str] = None
    
    # 应用配置
    app_env: str = "development"
    log_level: str = "INFO"
    
    class Config:
        env_file = ".env"
        case_sensitive = False

settings = Settings()
```

```typescript
// config.ts - TypeScript配置管理
import { z } from 'zod';
import dotenv from 'dotenv';

dotenv.config();

const configSchema = z.object({
  openaiApiKey: z.string().min(1),
  openaiApiBase: z.string().url().default('https://api.openai.com/v1'),
  anthropicApiKey: z.string().optional(),
  appEnv: z.enum(['development', 'production']).default('development'),
  logLevel: z.enum(['DEBUG', 'INFO', 'WARN', 'ERROR']).default('INFO'),
});

export const config = configSchema.parse({
  openaiApiKey: process.env.OPENAI_API_KEY,
  openaiApiBase: process.env.OPENAI_API_BASE,
  anthropicApiKey: process.env.ANTHROPIC_API_KEY,
  appEnv: process.env.APP_ENV,
  logLevel: process.env.LOG_LEVEL,
});
```

---

## 6. 本地LLM部署

### 6.1 Ollama（推荐）

```bash
# 1. 安装Ollama
# Mac/Linux: curl -fsSL https://ollama.com/install.sh | sh
# Windows: 从ollama.com下载安装

# 2. 启动服务
ollama serve

# 3. 拉取模型
ollama pull llama3
ollama pull mistral
ollama pull qwen2

# 4. 运行模型
ollama run llama3

# 5. API调用
curl http://localhost:11434/api/generate -d '{
  "model": "llama3",
  "prompt": "Hello, how are you?"
}'
```

### 6.2 LangChain集成Ollama

```python
from langchain_community.llms import Ollama
from langchain_community.chat_models import ChatOllama

# 文本生成
llm = Ollama(model="llama3")
response = llm.invoke("Tell me a joke")

# 对话模型
chat = ChatOllama(model="llama3")
response = chat.invoke("Hello!")
```

---

## 7. 向量数据库本地部署

### 7.1 ChromaDB（最简单）

```python
# 本地文件存储
from langchain_community.vectorstores import Chroma
from langchain_community.embeddings import OllamaEmbeddings

embeddings = OllamaEmbeddings(model="nomic-embed-text")

vectorstore = Chroma(
    embedding_function=embeddings,
    persist_directory="./chroma_db"
)

# 添加文档
vectorstore.add_texts(["Hello world", "AI is amazing"])

# 检索
results = vectorstore.similarity_search("AI", k=2)
```

### 7.2 Docker部署Milvus

```yaml
# docker-compose.yml
version: '3.8'

services:
  milvus:
    image: milvusdb/milvus:latest
    container_name: milvus-standalone
    ports:
      - "19530:19530"
      - "9091:9091"
    volumes:
      - ./milvus_data:/var/lib/milvus
    environment:
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000

  etcd:
    image: quay.io/coreos/etcd:latest
    ports:
      - "2379:2379"

  minio:
    image: minio/minio:latest
    command: server /data
    ports:
      - "9000:9000"
```

```bash
# 启动
docker-compose up -d

# Python连接
from pymilvus import connections
connections.connect("default", host="localhost", port="19530")
```

---

## 8. Docker开发环境

### 8.1 开发环境Dockerfile

```dockerfile
# Dockerfile - Python Agent开发环境
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    build-essential \
    curl \
    git \
    && rm -rf /var/lib/apt/lists/*

# 安装Python依赖
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制代码
COPY . .

# 开发模式启动
CMD ["python", "main.py"]
```

### 8.2 完整开发栈

```yaml
# docker-compose.dev.yml
version: '3.8'

services:
  # Agent应用
  agent-app:
    build: .
    volumes:
      - .:/app
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    depends_on:
      - chroma
      - ollama

  # 向量数据库
  chroma:
    image: chromadb/chroma:latest
    ports:
      - "8000:8000"
    volumes:
      - ./chroma_data:/chroma/data

  # 本地LLM
  ollama:
    image: ollama/ollama:latest
    ports:
      - "11434:11434"
    volumes:
      - ollama_data:/root/.ollama

  # 监控（可选）
  langfuse:
    image: langfuse/langfuse:latest
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=postgresql://postgres:postgres@db:5432/langfuse
    depends_on:
      - db

  db:
    image: postgres:15
    environment:
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: langfuse

volumes:
  ollama_data:
```

```bash
# 启动开发环境
docker-compose -f docker-compose.dev.yml up -d

# 查看日志
docker-compose logs -f agent-app

# 进入容器
docker-compose exec agent-app bash
```

---

## 9. 项目模板

### 9.1 Python项目结构

```
my-agent-project/
├── .env                    # 环境变量
├── .gitignore
├── requirements.txt
├── pyproject.toml          # 现代Python项目配置
│
├── src/
│   ├── __init__.py
│   ├── main.py             # 入口
│   ├── config.py           # 配置管理
│   │
│   ├── agents/             # Agent实现
│   │   ├── __init__.py
│   │   ├── base.py         # 基类
│   │   └── custom.py       # 自定义Agent
│   │
│   ├── tools/              # 工具
│   │   ├── __init__.py
│   │   └── custom_tools.py
│   │
│   ├── memory/             # 记忆管理
│   │   └── vector_store.py
│   │
│   └── utils/              # 工具函数
│       ├── prompts.py
│       └── helpers.py
│
├── tests/                  # 测试
│   ├── test_agents.py
│   └── test_tools.py
│
├── notebooks/              # Jupyter笔记本
│   └── experiments.ipynb
│
└── docker/
    ├── Dockerfile
    └── docker-compose.yml
```

### 9.2 TypeScript项目结构

```
my-agent-project/
├── .env
├── .gitignore
├── package.json
├── tsconfig.json
│
├── src/
│   ├── index.ts            # 入口
│   ├── config.ts           # 配置
│   │
│   ├── agents/             # Agent实现
│   │   ├── base.ts
│   │   └── custom.ts
│   │
│   ├── tools/              # 工具
│   │   └── custom.ts
│   │
│   ├── memory/             # 记忆
│   │   └── store.ts
│   │
│   └── utils/
│       └── helpers.ts
│
├── tests/
│   └── agent.test.ts
│
└── docker/
    └── Dockerfile
```

---

## 10. 快速启动模板

### 10.1 最小可运行示例

```python
# main.py - 最小Agent示例
from langchain_openai import ChatOpenAI
from langchain.agents import AgentExecutor, create_react_agent
from langchain.tools import Tool
from langchain import hub

# 1. 定义工具
def get_word_length(word: str) -> str:
    return f"单词 '{word}' 的长度是 {len(word)}"

tools = [
    Tool(
        name="WordLength",
        func=get_word_length,
        description="计算单词长度"
    )
]

# 2. 创建LLM
llm = ChatOpenAI(model="gpt-3.5-turbo", temperature=0)

# 3. 创建Agent
prompt = hub.pull("hwchase17/react")
agent = create_react_agent(llm, tools, prompt)
agent_executor = AgentExecutor(agent=agent, tools=tools, verbose=True)

# 4. 运行
result = agent_executor.invoke({"input": "hello这个单词有几个字母？"})
print(result["output"])
```

### 10.2 一键启动脚本

```bash
#!/bin/bash
# setup.sh - 一键环境搭建

echo "🚀 开始搭建Agent开发环境..."

# 检查Python
if ! command -v python3 &> /dev/null; then
    echo "❌ 请先安装Python 3.10+"
    exit 1
fi

# 创建虚拟环境
echo "📦 创建虚拟环境..."
python3 -m venv agent-env
source agent-env/bin/activate

# 安装依赖
echo "📥 安装依赖..."
pip install --upgrade pip
pip install -r requirements.txt

# 创建.env文件
if [ ! -f .env ]; then
    echo "📝 创建.env配置文件..."
    cp .env.example .env
    echo "⚠️  请编辑.env文件，填入你的API密钥"
fi

# 创建目录结构
echo "📁 创建项目结构..."
mkdir -p src/agents src/tools src/memory tests notebooks

echo "✅ 环境搭建完成！"
echo "运行以下命令开始开发:"
echo "  source agent-env/bin/activate"
echo "  python src/main.py"
```

---

## 11. 常见问题

### Q1: 依赖冲突怎么办？

```bash
# 使用pip-tools管理依赖
pip install pip-tools

# 创建requirements.in
echo "langchain>=0.2.0" > requirements.in
echo "openai>=1.0.0" >> requirements.in

# 编译生成requirements.txt
pip-compile requirements.in

# 安装
pip-sync
```

### Q2: API调用失败？

```python
# 添加重试机制
from tenacity import retry, stop_after_attempt, wait_exponential

@retry(stop=stop_after_attempt(3), wait=wait_exponential(multiplier=1, min=4, max=10))
def call_llm(prompt):
    return llm.invoke(prompt)
```

### Q3: 内存不足？

```python
# 流式处理减少内存占用
from langchain.callbacks.streaming_stdout import StreamingStdOutCallbackHandler

llm = ChatOpenAI(
    model="gpt-3.5-turbo",
    streaming=True,
    callbacks=[StreamingStdOutCallbackHandler()]
)
```

---

*下一章节: 架构选型对比 →*
