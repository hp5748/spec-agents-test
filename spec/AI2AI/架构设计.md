# 架构设计文档

> 本文档整合了系统架构、代码结构、实现决策等内容

---

## 一、系统整体架构

```mermaid
graph TB
    subgraph 客户端层
        UI[Web界面<br/>index.html]
    end

    subgraph API服务层
        MAIN[FastAPI服务<br/>main.py]
        CORS[CORS中间件]
        STREAM[流式响应<br/>SSE]
    end

    subgraph Agent智能层
        AGENT[智能客服Agent<br/>agent.py]
        INTENT[意图识别模块]
        ROUTER[路由分发模块]
        LLM[LLM调用模块]
        SKILLS[Skills技能模块]
    end

    subgraph 工具层
        TOOLS[工具管理<br/>tools.py]
        ORDER[订单查询工具]
        PRODUCT[商品搜索工具]
        TICKET[工单创建工具]
    end

    subgraph 记忆层
        MEMORY[对话记忆<br/>memory.py]
        HISTORY[历史管理]
        WINDOW[窗口控制]
    end

    subgraph 配置层
        CONFIG[配置管理<br/>config.py]
        PROMPTS[提示词<br/>prompts.py]
        ENV[环境变量<br/>.env]
    end

    subgraph 外部服务
        SILICON[硅基流动API<br/>DeepSeek-V3.2]
    end

    UI -->|HTTP/SSE| MAIN
    MAIN --> CORS
    MAIN --> STREAM
    MAIN --> AGENT

    AGENT --> INTENT
    AGENT --> ROUTER
    AGENT --> LLM
    AGENT --> SKILLS

    ROUTER --> TOOLS
    TOOLS --> ORDER
    TOOLS --> PRODUCT
    TOOLS --> TICKET

    AGENT <--> MEMORY
    MEMORY --> HISTORY
    MEMORY --> WINDOW

    CONFIG --> AGENT
    CONFIG --> TOOLS
    PROMPTS --> AGENT
    ENV --> CONFIG

    LLM -->|API调用| SILICON
```

---

## 二、核心模块依赖关系

```mermaid
graph LR
    subgraph 入口模块
        main[main.py]
    end

    subgraph 核心模块
        agent[agent.py]
        config[config.py]
        memory[memory.py]
        tools[tools.py]
        prompts[prompts.py]
    end

    subgraph 技能模块
        skills[skills/]
        registry[registry.py]
        base[base.py]
    end

    main --> agent
    main --> memory

    agent --> config
    agent --> memory
    agent --> tools
    agent --> prompts
    agent --> skills

    tools --> config
    skills --> registry
    registry --> base

    style main fill:#e1f5fe
    style agent fill:#fff3e0
    style config fill:#e8f5e9
    style skills fill:#f3e5f5
```

---

## 三、数据流向图

```mermaid
flowchart TD
    A[用户输入] --> B[FastAPI接口]
    B --> C{获取/创建Agent实例}
    C -->|新会话| D[创建新Agent]
    C -->|已有会话| E[获取缓存Agent]
    D --> F[Agent处理]
    E --> F

    subgraph Agent处理流程
        F --> G[加载对话历史]
        G --> H[意图识别]
        H --> I{意图类型}
        I -->|匹配技能| J[Skills处理]
        I -->|无技能| K[工具处理]
        I -->|通用| M[直接响应]
    end

    J --> N[技能执行结果]
    K --> N
    N --> O[LLM生成响应]
    M --> O

    O --> P[保存对话历史]
    P --> Q{响应类型}
    Q -->|普通| R[JSON响应]
    Q -->|流式| S[SSE流式响应]
    R --> T[返回用户]
    S --> T
```

---

## 四、文件结构

```
project/
├── src/                        # 源代码目录
│   ├── main.py                # [入口] FastAPI应用
│   ├── agent.py               # [核心] 智能Agent
│   ├── config.py              # [配置] 配置和常量
│   ├── tools.py               # [工具] 工具定义
│   ├── memory.py              # [记忆] 对话记忆
│   ├── prompts.py             # [提示词] Prompt模板
│   └── skills/                # [技能] 技能模块
│       ├── __init__.py
│       ├── base.py            # 技能基类
│       ├── registry.py        # 技能注册中心
│       └── implementations/   # 技能实现
│
├── skills/                     # 技能配置目录
│   ├── skills.yaml            # 技能配置文件
│   └── order-assistant/       # 订单助手技能
│       ├── SKILL.md
│       └── scripts/executor.py
│
├── static/                     # 静态资源
│   └── index.html             # Web聊天界面
│
├── spec/                       # 项目文档
│   ├── Me2AI/                 # 需求文档
│   └── AI2AI/                 # 技术文档
│
├── .env                       # 环境变量
├── requirements.txt           # Python依赖
└── README.md                  # 项目说明
```

---

## 五、核心类设计

```mermaid
classDiagram
    class Config {
        +API_KEY: str
        +BASE_URL: str
        +MODEL_NAME: str
        +TEMPERATURE: float
        +MAX_TOKENS: int
        +SKILLS_ENABLED: bool
    }

    class IntentType {
        <<enumeration>>
        ORDER_QUERY
        PRODUCT_CONSULT
        COMPLAINT
        GENERAL_QA
    }

    class CustomerServiceAgent {
        -session_id: str
        -llm: ChatOpenAI
        -memory: ConversationMemory
        +__init__(session_id)
        +classify_intent(user_input) IntentType
        +process_with_skill(user_input, intent) str
        +process_with_tool(user_input, intent) str
        +chat(user_input) Dict
        +chat_stream_simple(user_input) Generator
    }

    class BaseSkill {
        <<abstract>>
        +name: str
        +description: str
        +version: str
        +required_tools: List
        +supported_intents: List
        +execute(context) SkillResult
        +execute_with_logging(context) SkillResult
    }

    class SkillRegistry {
        -_skills: Dict
        -_intent_map: Dict
        +register(skill_class, config)
        +get_skill(name, tools, llm)
        +get_skill_by_intent(intent)
        +auto_discover()
    }

    Config <.. CustomerServiceAgent : uses
    IntentType <.. CustomerServiceAgent : uses
    BaseSkill <.. SkillRegistry : manages
    CustomerServiceAgent --> SkillRegistry : uses
```

---

## 六、技术决策记录

### 1. LLM 选择
**决策**: 使用硅基流动的 DeepSeek-V3.2

**原因**:
1. OpenAI 兼容接口，降低迁移成本
2. DeepSeek 中文能力强，性价比高
3. 硅基流动提供稳定的 API 服务

**实现**:
```python
self.llm = ChatOpenAI(
    model="deepseek-ai/DeepSeek-V3.2",
    api_key=Config.API_KEY,
    base_url="https://api.siliconflow.cn/v1",
    request_timeout=120,
    max_retries=1,
)
```

---

### 2. 流式响应实现
**决策**: 使用 Server-Sent Events (SSE)

**原因**:
1. SSE 比 WebSocket 更简单，适合单向数据流
2. 原生 HTTP 协议，无需额外握手
3. FastAPI 原生支持 StreamingResponse

**SSE 格式**:
```
data: {"type": "intent", "intent": "xxx", "intent_name": "xxx"}\n\n
data: {"type": "content", "content": "文本块"}\n\n
data: {"type": "done"}\n\n
```

---

### 3. Skills 技能系统
**决策**: 引入 Skills 作为 Tools 的高级抽象

**设计**:
- 每个技能独立目录，包含 SKILL.md 和执行器
- 支持多步骤操作、专业知识、流式输出
- 通过 skills.yaml 配置管理
- 支持热加载

---

## 七、技术栈

| 层级 | 技术选型 | 用途 |
|------|----------|------|
| 前端 | HTML5 + CSS3 + JavaScript | Web聊天界面（黑科技风格） |
| 后端框架 | FastAPI | 高性能API服务 |
| LLM框架 | LangChain | 工具调用和链式调用 |
| 模型 | DeepSeek-V3.2 | 语言理解与生成 |
| API提供 | 硅基流动 | 模型推理服务 |
| 流式传输 | SSE | 实时响应 |
| 技能系统 | Skills | 高级能力抽象 |

---

*文档更新时间: 2026-03-13*
